library phosphor_flutter;

import 'package:flutter/widgets.dart';

// IconData is final in recent Flutter versions. Keep the public type name used
// by the generated API without subclassing the framework class.
typedef PhosphorIconData = IconData;
