<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ (cloud backup + device-to-device transfer). Exclude the Flutter
  SharedPreferences file so a previously-saved theme choice ("isDarkMode") is NOT
  restored on reinstall — a fresh install then follows the phone's current
  light/dark setting.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="FlutterSharedPreferences.xml" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="FlutterSharedPreferences.xml" />
    </device-transfer>
</data-extraction-rules>
